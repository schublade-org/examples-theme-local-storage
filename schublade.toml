# Theme persisted on localStorage, then mirrored for CSS.
catalog = "./catalog.toml"

[server]
host = "0.0.0.0"
port = 47305

[theme]
trigger = "local-storage"
key = "theme"
light = "light"
dark = "dark"

[a11y]
enabled = true
rules = ["button-name"]
