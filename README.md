# Theme · local-storage

`trigger = "local-storage"` with `key = "theme"`. The preview writes `localStorage.theme` when you toggle light/dark.

```bash
npx schublade serve --config ./schublade.toml
# or ./run.sh
```

http://127.0.0.1:47305
